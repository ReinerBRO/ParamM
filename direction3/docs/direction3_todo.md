# direction3 TODO（基于已复制文档提炼）

1. 定义路由器 state 特征集合（题目长度、失败类型、反思轮次、检索相似度统计）并落地到特征提取脚本。
   验收标准：完成特征字段清单与提取代码，随机抽检 20 条样本特征完整率 100%。

2. 从历史 first_stage_log/second_stage_log 回放构建监督样本，形成 state->memory_mix->outcome 数据。
   验收标准：生成可复现数据集文件，包含训练/验证划分与字段说明，样本量与日志统计一致。

3. 实现 `memory_router/dataset_builder.py`，支持一键产出训练集与验证集。
   验收标准：脚本单次运行成功，输出路径固定，且训练/验证集无重复题目泄漏。

4. 实现轻量 MLP 路由器并输出三记忆权重与置信度。
   验收标准：模型前向输出维度正确（三权重+置信度），权重归一化且置信度范围在 [0,1]。

5. 完成路由器训练与 checkpoint 保存（router.ckpt）并记录跨数据集指标。
   验收标准：训练日志包含 loss/验证指标，产出 `router.ckpt`，并附 HumanEval↔MBPP 交叉评估结果。

6. 在 `paramAgent.py` 检索前接入 router 推理，执行加权融合检索。
   验收标准：开启开关后在线推理可稳定运行，日志出现 `router_mix` 且检索流程无报错。

7. 增加 `--router_enable` 与 `--router_conf_threshold` 参数，并实现低置信 fallback 固定策略。
   验收标准：不同阈值下 `fallback_flag` 按预期变化，低置信样本回退生效且结果可复现。

8. 增加可解释日志字段：`router_mix`、`router_conf`、`fallback_flag`。
   验收标准：每道题日志均包含三字段，字段格式统一，可直接用于离线分析脚本。

9. 以已复制实验基线为参照建立评测门槛：Phase2 至少一个数据集 Pass@1 +1.0 绝对点且不双退化。
   验收标准：评测报告明确列出基线（如 HumanEval 81.10%、MBPP 75.31%）与改进值，并满足门槛。

10. 完成稳定性与泛化验收：跨数据集退化不超过 1.0 绝对点并输出最终结论。
    验收标准：提交最终验收文档，含配置、结果表与结论，满足“不可双主数据集同时显著退化”。
